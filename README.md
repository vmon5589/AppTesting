"Currently, this crashes as a result of UserProfile within the page.tsx file in the directory: app/(auth)/onboarding/page.tsx"
"The error is exactly: "ClerkRuntimeError: Clerk: <UserProfile/> cannot render unless a user is signed in. Since no user is signed in, this is no-op."

